{
  "name": "tanda-server",
  "version": "1.0.0",
  "description": "Accounts and progress sync for the TANDA app",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
     "start": "node --experimental-sqlite server.js",
    "dev": "node --watch server.js",
    "migrate": "node db.js --migrate"
  },
  "engines": { "node": ">=22" },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "jsonwebtoken": "^9.0.2"
  }
}
